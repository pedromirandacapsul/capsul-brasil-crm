# 🚀 EXEMPLO DE CONFIGURAÇÃO PARA PRODUÇÃO COM AMAZON SES

# Database (usar PostgreSQL em produção)
DATABASE_URL="postgresql://usuario:senha@localhost:5432/capsul_prod"

# Redis (requerido para sessões e cache)
REDIS_URL="redis://localhost:6379"
QUEUE_REDIS_URL="redis://localhost:6379"

# NextAuth (gerar nova secret key)
NEXTAUTH_URL="https://capsul.com.br"
NEXTAUTH_SECRET="sua-super-secret-key-para-producao-muito-longa-e-segura"

# ===== EMAIL CONFIGURATION - AMAZON SES =====
SMTP_PROVIDER="ses"
SMTP_FROM="noreply@capsul.com.br"  # ← USAR SEU DOMÍNIO VERIFICADO
SMTP_FROM_NAME="Capsul Brasil CRM"

# Amazon SES - CONFIGURAR COM SUAS CREDENCIAIS REAIS
AWS_REGION="us-east-1"
AWS_SES_ACCESS_KEY="AKIA1234567890EXAMPLE"      # ← SUA ACCESS KEY
AWS_SES_SECRET_KEY="wJalrXUtnFEMI/K7MDENGexample" # ← SUA SECRET KEY
SES_SMTP_HOST="email-smtp.us-east-1.amazonaws.com"

# App Settings
NODE_ENV="production"
NEXT_PUBLIC_APP_URL="https://capsul.com.br"

# Security
SECURITY_HEADERS_ENABLED=true
CSP_REPORT_URI="https://capsul.com.br/api/csp-report"

# Analytics (configurar com suas chaves reais)
ANALYTICS_MOCK=0
GTM_ID="GTM-XXXXXXX"
GA4_ID="G-XXXXXXXXXX"
META_PIXEL_ID="123456789012345"

# File Upload (usar S3 em produção)
MAX_FILE_SIZE=10485760  # 10MB
UPLOAD_DIR="./uploads"

# Rate Limiting
RATE_LIMIT_MAX=1000
RATE_LIMIT_WINDOW=900000

# Metabase (se usando)
BI_EMBED_SECRET="sua-metabase-embed-secret"
METABASE_URL="https://analytics.capsul.com.br"

# ===== OUTRAS CONFIGURAÇÕES OPCIONAIS =====

# SendGrid (alternativa ao SES)
# SMTP_PROVIDER="sendgrid"
# SENDGRID_API_KEY="SG.sua-api-key-do-sendgrid"

# Gmail (para testes apenas)
# SMTP_PROVIDER="gmail"
# SMTP_USER="seu-email@gmail.com"
# SMTP_PASS="sua-app-password"

# SMTP Customizado
# SMTP_PROVIDER="custom"
# SMTP_HOST="smtp.seuproveedor.com"
# SMTP_PORT="587"
# SMTP_SECURE="false"
# SMTP_USER="seu-usuario"
# SMTP_PASS="sua-senha"

# ===== EXEMPLO DE CONFIGURAÇÃO DNS NECESSÁRIA =====
#
# Para o domínio capsul.com.br, adicione estes registros DNS:
#
# VERIFICAÇÃO DO DOMÍNIO (SES Console te dará valores específicos):
# TXT  _amazonses.capsul.com.br.  "AB1234567890CDEF..."
#
# DKIM (SES Console te dará 3 registros como estes):
# CNAME abc123._domainkey.capsul.com.br. abc123.dkim.amazonses.com.
# CNAME def456._domainkey.capsul.com.br. def456.dkim.amazonses.com.
# CNAME ghi789._domainkey.capsul.com.br. ghi789.dkim.amazonses.com.
#
# SPF (protege contra spoofing):
# TXT capsul.com.br. "v=spf1 include:amazonses.com ~all"
#
# DMARC (política de email):
# TXT _dmarc.capsul.com.br. "v=DMARC1; p=quarantine; rua=mailto:dmarc@capsul.com.br"
#